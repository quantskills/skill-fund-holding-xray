# Local report examples

Run `python scripts/fund_xray.py --symbol <fund-symbol> --save <report.html>` with your authorized data access to generate a local example. Generated reports and screenshots may contain third-party market data and should not be committed without confirmed redistribution rights.
